<UserControl x:Class="Accounting101.Views.Update.UpdateAccountEntriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:read="clr-namespace:Accounting101.Views.Read"
             xmlns:update="clr-namespace:Accounting101.Views.Update"
             xmlns:controls="clr-namespace:Accounting101.Controls"
             mc:Ignorable="d"
             d:Background="Teal"
             Background="Transparent"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <read:AccountHeaderView x:Name="AccountHeaderView" Grid.Row="0" Background="Transparent" />
        <update:UpdateAccountTransactionsView x:Name="TransactionList" Grid.Row="1" Background="Transparent" />
        <controls:FastEntryControl x:Name="FastEntryControl" Grid.Row="2" Background="Transparent" />
    </Grid>
</UserControl>
<UserControl x:Class="Accounting101.Views.StateSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting101.Views"
             xmlns:viewModels="clr-namespace:Accounting101.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:StateSelectorViewModel}"
             d:DesignHeight="22" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="State:" Padding="5, 3, 5, 3" d:Background="White" />
        <ComboBox
            Grid.Column="1"
            x:Name="StateSelector"
            ItemsSource="{Binding States}"
            SelectedValue="{Binding SelectedState, Mode=TwoWay}" />
    </Grid>
</UserControl>

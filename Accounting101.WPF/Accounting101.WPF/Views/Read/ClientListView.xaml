<UserControl x:Class="Accounting101.WPF.Views.Read.ClientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:read1="clr-namespace:Accounting101.WPF.Views.Read"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance read1:ClientListView}"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource MahApps.Brushes.Accent}"
             SizeChanged="ClientListViewSizeChanged">
    <Grid  Background="{DynamicResource MahApps.Brushes.Accent}">
        <ScrollViewer Background="{DynamicResource MahApps.Brushes.Accent}" VerticalScrollBarVisibility="Auto">
            <ListView Background="{DynamicResource MahApps.Brushes.Accent}" ItemsSource="{Binding ClientTiles}" />
        </ScrollViewer>
    </Grid>
</UserControl>